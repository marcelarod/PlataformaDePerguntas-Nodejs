<%- include('partials/header.ejs') %>
<body>
    <%- include('partials/navbar.ejs') %>
    <div class="container">
        <h1> Perguntas </h1>
        <hr>
        <a href="ask" class="btn btn-dark" >Faça sua pergunta</a>
        <br>
        <% ask.forEach(ask =>{%>
            <br>
            <div class="card">
            <div class="card-body">
                <h3><%= ask.title %></h3>
            </div>
            <div class="card-footer">
                <a href="answer/<%= ask.id %>"> <button class="btn btn-dark">Responder</button></a> 
            </div>
          </div>
       <% }) %>

    </div>
</body>

 <%- include('partials/footer.ejs') %>

